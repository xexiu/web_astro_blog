# Configure Netlify builds.
[build]
  publish = "dist/"
  command = "astro build --no-hmr --adapter @astro/node"

# Request bots not to index the admin section.
[[headers]]
  for = "/admin/*"
  [headers.values]
    X-Robots-Tag = "noindex"