---
interface Props {
  text?: string;
  classes?: string;
  title?: string;
}
const { text, title = 'Botón', classes = '', ...rest } = Astro.props;
---

<button
   {...rest}
   aria-label={title}
   type="button"
   class:list={['btn-clean', classes]}
>
   <slot name="button-before-text" />
   {text}
   <slot name="button-after-text" />
</button>
