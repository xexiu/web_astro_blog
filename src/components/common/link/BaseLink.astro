---
export interface Props {
   text?: string;
   href: string;
   customStyle?: any;
   icon?: {
      name: string;
      side: 'left' | 'right';
      width?: string | '24';
      height?: string | '24';
      class?: string;
   };
   isFilled?: boolean;
   borderVisible?: boolean;
   classes?: string;
   target?: string;
}

const { href, classes, customStyle, isFilled, borderVisible, ...rest } =
    Astro.props;
---

<a
   href={href}
   class:list={[
      classes,
      { filled: isFilled, bordered: borderVisible }
   ]}
   style={customStyle}
   {...rest}
>
   <slot />
</a>
