---
import type { LinkProps } from 'types/interfaces';
import ExternalLink from './ExternalLink.astro';
import InternalLink from './InternalLink.astro';

const {
   text,
   href,
   customStyle,
   icon,
   isFilled = true,
   borderVisible = false,
   isExternalLink = false,
   classes = 'link',
   ...rest
} = Astro.props as LinkProps;


function isInternalURLValid(url: string) {
   const whiteListDomains = ['https://xexiu', 'http://localhost', '/'];

   for (const domain of whiteListDomains) {
      if (url.startsWith(domain)) {
         return true;
      }
   }

   return false;
}

const isInternalUrl = isInternalURLValid(href);

---

{
   isExternalLink ? (
      <ExternalLink
         {href}
         {text}
         {icon}
         classes={`${classes} ${isInternalUrl ? 'internal-link' : 'external-link'}`}
         {customStyle}
         {isFilled}
         {borderVisible}
         {...rest}
      >
         <slot />
      </ExternalLink>
   ) : (
      <InternalLink
         {href}
         {text}
         {icon}
         classes={`${classes} ${isInternalUrl ? 'internal-link' : 'external-link'}`}
         {customStyle}
         {isFilled}
         {borderVisible}
         {...rest}
      >
         <slot />
      </InternalLink>
   )
}
