---
interface Props {
    classes?: string;
    style?: string;
    tag: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6';
}
const { tag = Element, classes, ...rest } = Astro.props as Props;
const Tag = tag;
---

<Tag class:list={[classes]} {...rest}>
   <slot />
</Tag>