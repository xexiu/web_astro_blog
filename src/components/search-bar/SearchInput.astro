<input
   type="text"
   class="input-search"
   placeholder="Que es js?..."
   aria-label="Busca los artículos dentro de la web"
   role="search"
   pattern=".*\S.*"
   required
/>

<style is:inline>
    .input-search {
        border-radius: 0;
        -webkit-appearance: none;
        -webkit-border-radius: 0;
        opacity: 0;
        visibility: hidden;
        height: 0;
        width: 100vw;
        background-color: #222;
        border-bottom: 2px solid rgb(255 255 255 / 50%);
        position: absolute;
        top: 62px;
        left: -120px;
        border-style: none;
        padding: 10px;
        font-size: var(--fs-sm);
        letter-spacing: 2px;
        outline: none;
        color: #fff;
        transition:
            opacity 0.3s,
            height 0.3s;

        &::placeholder {
            color: rgb(255 255 255 / 50%);
            font-size: var(--fs-sm);
            letter-spacing: 2px;
            font-weight: 100;
        }
    }
    @media screen and (width <= 500px) {
        .input-search {
            left: -110px;
        }
    }
</style>
